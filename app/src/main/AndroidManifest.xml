<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cmsc436.mobilesurvey">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.InitialActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.MainActivity"/>
        <activity android:name=".main.LoginActivity" />
        <activity android:name=".main.RegistrationActivity" />
        <activity android:name=".main.DashboardActivity"></activity>
        <activity android:name=".main.ResponsesActivity"></activity>
        <activity android:name=".forms.CreateSurveyActivity"></activity>
        <activity android:name=".forms.SurveyActivity"></activity>
        <activity android:name=".utils.ScanActivity"> </activity>
        <activity android:name=".main.ResponseDetailActivity"> </activity>
    </application>

</manifest>